---
applications:
  .:
    name: it-desparche
    framework:
      name: node
      info:
        mem: 64M
        description: Node.js Application
        exec: 
    infra: hp
    url: ${name}.${target-base}
    mem: 1G
    instances: 1
    services:
      desparche:
        type: mongodb
